# chat（FastAPI 后端）的运行时配置模板
#
# 真实文件：~/.config/chat/chat.env
# 读取方　：chat.service 的 EnvironmentFile=%h/.config/chat/chat.env
#
# 当前**没有任何变量**。这个文件真实存在的意义只是给 EnvironmentFile 留一个落点 ——
# 所以它是空的，不是漏了。
#
# 公网密码不在这里：由 cpolar 边缘负责，见 config/README.md 的映射表。
# 改密码也不需要碰这个文件（只改 cpolar.yml 的 auth，然后 restart cpolar）。
#
# 将来要加变量就写在下面，例如：
# CHAT_SOMETHING=value
